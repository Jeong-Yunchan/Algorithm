function solution(n, times) {
  var answer = 0;
  let time = times.reduce((acc,cur) => {
    if(!acc[cur]){
      acc[cur] 
    }
  })
  return answer;
}

const n = 6;
const times = [7,10];
solution(n,times);