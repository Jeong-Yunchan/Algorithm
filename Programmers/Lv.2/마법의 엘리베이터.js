function solution(storey) {
  var answer = 0;
  let arr = storey.toString().split('').map(v => Number(v));
  console.log(arr);
  const fuc = (count, num, stone) =>{

  }
  console.log(answer);
  return answer;
}
const storey = 11;
solution(storey);
